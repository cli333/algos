// determine if a string has all unique chars
// cannot use extra space/data structures

const unique = string => {
  // brute force
  // for (let i = 0; i < string.length; i++) {
  //   for (let j = 1; j < string.length; j++) {
  //     if (string[i] === string[j]) return false;
  //   }
  // }
  // return true;
};

console.log(unique("abacd"));

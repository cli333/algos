// an array contains elements that appear twice except for one
// find the unique
// no extra space, no hash map

const single = arr => {
  // bit manipulation
  // lookup
};

console.log(single([2, 2, 1]));
